When to use linked list

Array VS Linked list
1. Array: add or remove non-edge elements
2. Linked List: add or remove first or last element, just change the pointer

use Object to realize linked list

prototype of JS

obj -> Object.prototype -> null
func -> Function.prototype -> Object.prototype -> null
arr -> Array.prototype -> Object.prototype -> null

use in Json